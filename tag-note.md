# tag note
 
a tag marks an important commit
a branch moves as new commits are added
a tag usually stays fixed on one commit
tags are often used for versions such as v1.0 or v2.0
git tag lists local tags
git tag -a creates an annotated tag
git push origin tag-name pushes one tag to github
